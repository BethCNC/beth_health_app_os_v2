rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down everything by default. Server-side/admin workflows bypass via Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
