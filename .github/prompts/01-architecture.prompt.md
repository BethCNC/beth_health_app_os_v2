Follow the architecture rules.

- App Router pages and layouts in src/app
- Shared UI in src/components
- Domain types in src/lib/domain
- Data access in src/lib/data only
- No direct Firestore calls from UI
